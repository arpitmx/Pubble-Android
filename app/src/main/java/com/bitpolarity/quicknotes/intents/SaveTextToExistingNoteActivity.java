package com.bitpolarity.quicknotes.intents;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

import com.bitpolarity.quicknotes.R;

public class SaveTextToExistingNoteActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_save_text_to_existing_note);
    }
}